[package]
name = "koala-tcp_rpc_adapter-plugin"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["dylib"]

[dependencies]
tcp_rpc_adapter = { path = "..", package = "koala-tcp_rpc_adapter" }
