[package]
name = "phoenix-rpc_adapter-plugin"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["cdylib"]

[dependencies]
rpc_adapter = { path = "..", package = "phoenix-rpc_adapter" }
